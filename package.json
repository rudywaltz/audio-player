{
  "name": "audio-player",
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "@esm-bundle/chai": "^4.3.4-fix.0",
    "@web/dev-server": "^0.1.34",
    "@web/test-runner": "^0.14.0",
    "@web/test-runner-commands": "^0.6.5",
    "@web/test-runner-playwright": "^0.8.10",
    "sinon": "^14.0.1"
  },
  "scripts": {
    "dev": "web-dev-server --node-resolve --open --watch --app-index src/index.html",
    "test": "web-test-runner src/**/*.test.js --node-resolve  --playwright",
    "test:watch": "web-test-runner src/**/*.test.js --node-resolve  --playwright --watch"
  },
  "author": "rudywaltz",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "uhtml": "^3.1.0"
  }
}
